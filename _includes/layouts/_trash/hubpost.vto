---
layout: layouts/post.vto
---

<div class="breadcrumb">
    {{ url.split("/").filter(part => part).map((part, i, arr) => {
        const path = "/" + arr.slice(0, i + 1).join("/");
            return `<a href="${path}">${part}</a>`;
    }).join(" > ") }}
</div>

<article>
    {{content}}    
</article>